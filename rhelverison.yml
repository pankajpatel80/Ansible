---

 - hosts: all
   tasks:

#    - name: check the RHEL version
#      shell: cat /etc/redhat-release
#      register: version

    - name: Collect OS version
      local_action: shell echo " {{ ansible_hostname }},{{ ansible_lsb.release }},{{ ansible_virtualization_type }},{{ ansible_default_ipv4.address }},{{ ansible_cmdline.BOOT_IMAGE }},{{ ansible_memtotal_mb }},{{ facter_system_uptime.uptime }} "  >> /root/pankaj/logs/log.txt
